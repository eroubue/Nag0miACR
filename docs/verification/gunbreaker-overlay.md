# 枪刃悬浮控制窗验证记录

## 实现范围

仅 Nag0miACR：枪刃的生命周期、设置绘制、独立窗口、模式配置和逻辑测试。保留 `ContentScope = AcrContentScope.HighEnd`；预设只调整既有参数与 QT。没有新增自动拉怪、减伤或盾姿的战斗逻辑，没有修改钐镰客或主插件源码。

激活后注册固定三按钮竖条和独立设置窗，设置每次激活默认关闭。原控制窗 `Plugin.Instance.SettingsWindow` 临时退出 WindowSystem，并在退出时恢复此前的注册／可见状态。原 QT 窗口不参与本次窗口管理。

配置位于宿主 `ConfigDirectory/Nag0mi.Gunbreaker.json`，包含版本、三套职业参数及 QT、当前模式、吸附侧与归一化坐标。每次编辑立即更新内存快照，磁盘写入限频到约 400ms；模式切换、拖动结束、设置关闭与退出时刷新保存。损坏文件另存为 `.corrupt-时间-随机标识.bak` 后回退默认值；读取或备份失败时保留原文件，本次仅使用内存配置。

## 可复现命令

在仓库根目录执行，输出和中间文件都位于工作区：

```powershell
dotnet run --project Tests/Nag0mi.LogicTests.csproj --artifacts-path artifacts/tests --no-launch-profile
dotnet build Nag0mi.csproj -c Release -p:OutputPath=artifacts/build/ -p:BaseIntermediateOutputPath=artifacts/obj/build/ --nologo
```

逻辑测试直接编译纯逻辑源码，不依赖游戏或 Dalamud 运行时。覆盖所有宿主状态／停手组合下的按钮操作、首次模式创建、所有参数应用、模式隔离和往返切换、外部 QT 同步及白名单、文件保存和损坏备份、归一化位置和吸附边界，以及窗口重复激活／清理／注册失败回滚／原可见状态恢复。测试临时文件位于测试输出目录。

## 本次自动验证结果

- 逻辑测试：275 项断言通过，退出码 0。
- Release 构建：0 错误、38 个警告，退出码 0；警告来自已有战斗／起手代码中的可空引用，与接入窗口前的构建一致。
- 产物：`artifacts/build/Nag0mi.dll`。没有写入运行中的插件输出目录。
- 独立代码审查：已修正 ForceMainWindow 的坐标换算、上一帧悬停影响背景拖动、失焦无效鼠标坐标、小视口字体与吸附比例问题。
- 变更范围检查：保留工作区已有修改；本次未编辑 Reaper 或宿主源码。

## 游戏内验收（尚未执行）

本次只完成自动化测试与编译验证。以下需要在真实 Dalamud / 游戏环境逐项检查，不能以编译通过代替：

- [ ] 首次激活：右侧中央，固定三个按钮，H 模式；完整设置隐藏；原控制窗隐藏，原 QT 窗口可用。
- [ ] 背景拖动：到左右边缘 32 × UI 缩放内当帧吸附，拖离解除；上下仅限位；按钮不会触发背景拖动。
- [ ] 分辨率、窗口位置与 Dalamud UI 缩放变化：保持吸附侧和相对高度，三个按钮完整可见，竖排方向不变。
- [ ] 齿轮：设置向画面内侧展开，约 560×500，可移动、调整大小和滚动；竖条尺寸和位置不变；关闭设置不影响执行状态。
- [ ] 绿／黄／灰与不同图标：运行左键暂停；暂停、关闭左键开启且解除停手；任意状态右键关闭；关闭后竖条仍可恢复 ACR。
- [ ] 从主插件 QT 或快捷键设置“停手”后显示暂停；模式切换不会改变运行开关或停手状态。
- [ ] N→H→C→N：分别修改通用、起手、开发参数和职业 QT，切换、刷新、重载后各模式保持独立；Normal 和 HighEnd 的三个辅助 QT 初值符合规格。
- [ ] 主插件 QT 窗口／快捷键修改职业 QT 后，切换模式或重新加载仍保留；其他职业 QT 和临时状态不会进入配置。
- [ ] 左右吸附、自由位置分别退出重载，恢复吸附侧和相对坐标；设置窗口每次激活都隐藏。
- [ ] 切职业、切 ACR、刷新、重复加载、退出插件：仅一套窗口，没有残留或重复注册；切出后原控制窗恢复；随后宿主可能按自身的职业／登录状态再次调整可见性。
- [ ] 在原控制窗打开／关闭两种初始状态下分别检查恢复；切出枪刃后仍能操作宿主设置和 QT。
- [ ] 使用损坏配置激活：原文件备份存在，回退 H 默认配置，下一次保存生成有效配置。
